{
  "name": "modelcompare",
  "version": "1.0.0",
  "description": "AI Model Comparison Dashboard",
  "main": "server.js",
  "scripts": {
    "start": "cd client && npm run build && cd .. && node server.js",
    "build": "cd client && npm run build",
    "postinstall": "cd client && npm install && npm run build",
    "test": "mocha tests/*.test.js --timeout 10000"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "cors": "^2.8.6",
    "express": "^4.22.2",
    "express-rate-limit": "^8.5.2",
    "express-validator": "^7.3.2",
    "helmet": "^8.2.0"
  },
  "keywords": [
    "chatbot",
    "ai",
    "nodejs",
    "express"
  ],
  "author": "Claude Code Assistant",
  "license": "MIT",
  "devDependencies": {
    "chai": "^6.2.2",
    "mocha": "^11.7.6",
    "supertest": "^7.2.2"
  }
}
